---
notify:
  - name: chrome_push
    platform: html5
    vapid_pub_key: !secret vapid_pub_key
    vapid_prv_key: !secret vapid_prv_key
    vapid_email: !secret vapid_email

  - name: hangouts
    platform: hangouts
    default_conversations:
      - id: !secret default_conversations