script:
  bike:
    alias: 'testing'
    sequence:
      - alias: 'Set date'
        service: input_datetime.set_datetime
        data:
          date:
            # '{{(now().strftime("%s") | int | timestamp_custom("%d.%m.%Y"))}}'
            '{{now()}}'
        entity_id:
          - input_datetime.last_bike_check
          
  
input_datetime:
  last_bike_check:
    name: Last bike check
    has_date: true
    has_time: false